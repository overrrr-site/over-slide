/**
 * Prompt utility helpers focused on token efficiency.
 */

/**
 * JSON stringify without indentation to avoid wasting prompt tokens.
 */
export function compactJsonForPrompt(value: unknown): string {
  try {
    const serialized = JSON.stringify(value);
    return typeof serialized === "string" ? serialized : String(value ?? "");
  } catch {
    return String(value ?? "");
  }
}

/**
 * 自然な日本語ライティングルール（JSON出力プロンプト用）。
 * AI臭さを排除し、人が書いたと感じる文章にするための制約。
 */
export const NATURAL_WRITING_RULES = `<writing_style>
出力テキストは「人が書いた自然な日本語」にすること。JSON内のテキストフィールドすべてに適用する。

【捏造禁止】入力にない数値・固有名・事例を足さない。曖昧な情報は曖昧なまま読みやすく書く。
【前置き・締め禁止】「結論から言うと」「本記事では」「以下で解説します」等の前置き宣言、「参考になれば幸いです」等の定型締めは書かない。
【安全クッション禁止】「一般的に」「多くの場合」「状況によって異なります」「一概には言えませんが」は削除する。
【抽象語→動詞化】「重要」「効果的」「最適」「本質」「メリット」だけで押し切らず、何がどうなるかを動詞中心で書く。
【反復禁止】同義語の羅列（重要・大切・欠かせない）、要約の繰り返し（「まとめると」「要するに」）は禁止。一度で言い切る。
【文のリズム】短文と長文を混ぜる。同じ構文パターンを連続させない。接続詞は必要最小限。
【人称統一】私／筆者／私たちを混在させない。
【記号制限】「」は引用・固有名詞のみ。（）は本文に組み込む。「： 」付きラベル羅列、／による並列、→の多用は禁止。
【読者への問いかけ禁止】確認・質問文を入れない。
</writing_style>`;

/**
 * 自然な日本語ライティングルール（マークダウン出力プロンプト用）。
 * セクション見出しは許可し、装飾的なマークダウンのみ禁止する。
 */
export const NATURAL_WRITING_RULES_MARKDOWN = `<writing_style>
出力テキストは「人が書いた自然な日本語」にすること。出力テキストすべてに適用する。

【捏造禁止】入力にない数値・固有名・事例を足さない。曖昧な情報は曖昧なまま読みやすく書く。
【前置き・締め禁止】「結論から言うと」「本記事では」「以下で解説します」等の前置き宣言、「参考になれば幸いです」等の定型締めは書かない。
【安全クッション禁止】「一般的に」「多くの場合」「状況によって異なります」「一概には言えませんが」は削除する。
【抽象語→動詞化】「重要」「効果的」「最適」「本質」「メリット」だけで押し切らず、何がどうなるかを動詞中心で書く。
【反復禁止】同義語の羅列（重要・大切・欠かせない）、要約の繰り返し（「まとめると」「要するに」）は禁止。一度で言い切る。
【文のリズム】短文と長文を混ぜる。同じ構文パターンを連続させない。接続詞は必要最小限。
【人称統一】私／筆者／私たちを混在させない。
【記号制限】「」は引用・固有名詞のみ。（）は本文に組み込む。「： 」付きラベル羅列、／による並列、→の多用は禁止。
【読者への問いかけ禁止】確認・質問文を入れない。
【マークダウン】セクション見出し（## ）は構造として使用可。本文中の**太字**や装飾的な箇条書きの乱用は禁止。
</writing_style>`;
